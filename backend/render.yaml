services:
  - type: web
    name: agentfloat-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: CIRCLE_ENV
        value: sandbox
      - key: PYTHON_VERSION
        value: 3.11.6
